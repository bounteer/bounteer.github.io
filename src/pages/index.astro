---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Hero from "../components/Hero.astro";
import Footer from "../components/Footer.astro";
import Products from "../components/Products.astro";
import FloatingButton from "../components/FloatingButton.astro";
---

<Layout
  title="Bounteer - 10x Your Recruiting Agency with Market Intelligence"
  name="Bounteer"
  description="Transform your recruiting agency with AI-powered market insights. Orbit Call automates outreach, Orbit Signal identifies high-intent opportunities from M&A intelligence. Become a 10x recruiter."
  url="https://bounteer.com"
  image_url="https://bounteer.com/og.png"
>
  <Header />
  <main>
    <Hero />
    <Products />
    <Footer />
  </main>

  <script>
    // Handle post-authentication redirect
    const urlParams = new URLSearchParams(window.location.search);
    const nextPath = urlParams.get("next");

    if (nextPath) {
      // Clean up the URL and redirect
      window.history.replaceState({}, document.title, window.location.pathname);
      window.location.href = nextPath;
    }
  </script>
</Layout>

<FloatingButton />
