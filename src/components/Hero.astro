<section class="relative pt-32 md:pb-8 md:pt-40 overflow-hidden hero-section">
  <div class="container-custom relative z-10">
    <div class="grid md:grid-cols-2 gap-12 items-center">
      <div class="slide-up" transition:animate="slide">
        <h1 class="mb-6 text-secondary-900 dark:text-white leading-tight">
          Your
          <span class="text-primary-800 dark:text-primary-400">Ultimate</span>
          <br />
          Sales Email Agent
        </h1>
        <p
          class="text-xl text-secondary-600 dark:text-secondary-300 mb-8 max-w-lg"
        >
          IsMail is your sales AI agent. Promoting your product to thousands of
          potential customers.
          <br />
          No upfront cost, only commission on results.
        </p>
        <div class="flex flex-wrap gap-4">
          <a href="/contact" class="btn-primary"> Contact IsMail </a>
        </div>
      </div>

      <!-- image location -->
      <div class="relative fade-in pb-12 md:pb-0" transition:animate="fade">
        <!-- Resize this wrapper to make both circle + avatar smaller -->
        <div class="relative w-[50%] max-w-[34rem] mx-auto md:ml-auto">
          <div
            class="relative aspect-square w-full flex items-center justify-center"
          >
            <!-- Orange circle -->
            <div
              class="absolute inset-0 rounded-full
               bg-gradient-to-br from-amber-300 via-orange-400 to-amber-500
               shadow-2xl"
            >
            </div>

            <!-- Avatar -->
            <img
              src="/ismail_square_clear.png"
              alt="IsMail avatar"
              class="relative
               rounded-full object-cover
               ring-8 ring-white dark:ring-secondary-900
               shadow-xl"
              loading="eager"
              decoding="async"
            />
          </div>
        </div>
      </div>

      <style>
        @keyframes float {
          0% {
            transform: translateY(0px);
          }
          50% {
            transform: translateY(-10px);
          }
          100% {
            transform: translateY(0px);
          }
        }
        .fade-in svg {
          animation: float 6s ease-in-out infinite;
        }
      </style>
    </div>
  </div>
</section>
