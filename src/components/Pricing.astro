---
import { EXTERNAL } from "../constant";
const plans = [
  {
    name: "Cost-Per-Sale (CPS) Affiliate",
    price: 0,
    description: "Zero-risk result-driven partnership",
    features: [
      "single referral link",
      "100K+ targeted mailing list per tag",
      "free onboarding",
    ],
    cta: "Start For Free",
    link: EXTERNAL.onboarding_form_url,
    popular: true,
    color: "accent",
  },
  {
    name: "Featured Newsletter & Blog Post",
    price: 299,
    description: "Ideal for news announcements",
    features: [
      "featured Bounteer newsletter",
      "featured Bounteer blogposts",
      "feedback analytics",
    ],
    cta: "Make A Request",
    link: "/contact",
    popular: false,
    color: "accent",
  },
  // {
  //   name: "Customized Mailing Service",
  //   price: 10000,
  //   description: "For teams with complex needs",
  //   features: [
  //     "ambient agent customization",
  //     "lead generation",
  //     "branding AB tests",
  //   ],
  //   cta: "Contact Account Manager",
  //   popular: false,
  //   color: "accent",
  // },
];
---

<section id="pricing" class="section bg-gray-50">
  <div class="container-custom">
    <div class="text-center max-w-3xl mx-auto mb-16">
      <span
        class="inline-block px-4 py-1 rounded-full bg-accent-100 text-accent-600 font-medium text-sm mb-4"
      >
        Partnership Plans
      </span>
      <h2 class="mb-6 text-gray-900">Simple, Transparent Partnership</h2>
      <p class="text-gray-600">
        Choose the plan that works best for your team.
      </p>
    </div>

    <div class="grid md:grid-cols-2 gap-8">
      {
        plans.map((plan, index) => (
          <div
            class={`card p-6 border ${plan.popular ? `border-${plan.color}-500` : "border-gray-200"} slide-up relative`}
            style={`animation-delay: ${index * 150}ms`}
          >
            {plan.popular && (
              <div
                class={`absolute top-0 right-0 bg-accent-600 text-white px-4 py-1 text-sm font-medium rounded-bl-lg`}
              >
                Most Popular
              </div>
            )}

            <h3 class="text-xl font-semibold mb-2 text-gray-900">
              {plan.name}
            </h3>
            <div class="mb-4">
              <span class="text-4xl font-bold text-gray-900">
                ${plan.price}
              </span>
              <span class="text-gray-500"> onboarding price</span>
            </div>
            <p class="text-gray-600 mb-6">{plan.description}</p>

            <ul class="space-y-3 mb-8">
              {plan.features.map((feature) => (
                <li class="flex items-start">
                  <svg
                    class={`w-5 h-5 text-${plan.color}-500 mr-2 shrink-0 mt-0.5`}
                    fill="currentColor"
                    viewBox="0 0 20 20"
                    xmlns="http://www.w3.org/2000/svg"
                  >
                    <path
                      fill-rule="evenodd"
                      d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                      clip-rule="evenodd"
                    />
                  </svg>
                  <span class="text-gray-600">{feature}</span>
                </li>
              ))}
            </ul>

            <a
              href={`${plan.link}`}
              class={`w-full btn ${plan.popular ? `bg-${plan.color}-600 hover:bg-${plan.color}-700 text-white` : "btn-outline"}`}
            >
              {plan.cta}
            </a>
          </div>
        ))
      }
    </div>

    <div class="mt-12 text-center">
      <p class="text-gray-600 mb-2">
        Need a custom plan for your specific needs?
      </p>
      <a href="/contact" class="text-accent-600 font-medium hover:underline"
        >Contact Bounteer</a
      >
    </div>
  </div>
</section>
